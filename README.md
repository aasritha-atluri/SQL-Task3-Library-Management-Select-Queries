# SQL-Task3-Library-Management-Select-Queries
SQL scripts for Task 3: Demonstrating SELECT queries with WHERE, AND, OR, LIKE, BETWEEN, ORDER BY, and LIMIT using the Library Management System database.
